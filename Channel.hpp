#pragma once

#include "Socket.hpp"
#include "server.hpp"

// typedef struct infoClient;

class Channel
{
    private:
        // std::map<int, infoClient>   _clients;
    public:
        Channel(void);
        ~Channel(void);
};
